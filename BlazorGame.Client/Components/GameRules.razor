@using BlazorGame.Client.Services
@using SharedModels.Entities
@inject GameService GameService
@namespace BlazorGame.Client.Components

@* Composant de règles de jeu *@

<div class="container mx-auto px-4 max-w-6xl flex flex-col gap-8">
    <div class="card text-center p-8 transform hover:scale-[1.02] transition-transform duration-300">
        <div class="flex justify-center items-center gap-4 mb-2">
            <h1 class="text-4xl font-medieval">Nouvelle Aventure</h1>
        </div>
        <p class="text-medieval-light opacity-80 text-lg font-medieval">Une nouvelle quête vous attend, brave aventurier</p>
    </div>

    <div class="card p-6 transform hover:scale-[1.01] transition-transform duration-300">
        <div class="text-center max-w-4xl mx-auto">
            <h2 class="text-3xl mb-6 font-medieval text-medieval-accent">Comment jouer ?</h2>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                <div class="text-left">
                    <h3 class="text-lg mb-2 font-medieval text-medieval-accent">L'Aventure</h3>
                    <p class="text-medieval-light text-sm">Explorez un donjon unique généré pour vous ! Traversez @(_config?.NumberOfRooms ?? 5) salles mystérieuses, chacune avec ses propres défis et trésors.</p>
                </div>
                <div class="text-left">
                    <h3 class="text-lg mb-2 font-medieval text-medieval-accent">Les Choix</h3>
                    <p class="text-medieval-light text-sm">Dans chaque salle, faites des choix cruciaux : Combattre des monstres, fouiller pour des trésors, ou fuir prudemment !</p>
                </div>
                <div class="text-left">
                    <h3 class="text-lg mb-2 font-medieval text-medieval-accent">Les Récompenses</h3>
                    <p class="text-medieval-light text-sm">Gagnez des points selon vos actions. Trouvez des trésors précieux, mais attention aux pièges mortels !</p>
                </div>
                <div class="text-left">
                    <h3 class="text-lg mb-2 font-medieval text-medieval-accent">La Gloire</h3>
                    <p class="text-medieval-light text-sm">Terminez l'aventure pour inscrire votre nom dans le classement des héros. Sauvegardez votre progression à tout moment.</p>
                </div>
            </div>

            <div class="mt-8">
                @if (_config != null)
                {
                    <p class="text-medieval-light text-center text-sm">Vous commencez avec <span class="text-medieval-accent font-medieval">@_config.StartingHealth points de vie</span>. Si vos points de vie tombent à 0, l'aventure se termine !</p>

                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mt-6">
                        <div class="card p-4 transform hover:scale-[1.01] transition-transform duration-300">
                            <h4 class="text-lg mb-2 font-medieval text-medieval-accent">Combattre</h4>
                            <ul class="text-medieval-light space-y-1 list-disc list-inside text-sm">
                                <li>Victoire : +@_config.MinCombatVictoryPoints à +@_config.MaxCombatVictoryPoints points</li>
                                <li>Défaite : @_config.MinCombatDefeatPoints à @_config.MaxCombatDefeatPoints points et @_config.MinCombatDefeatHealthLoss à @_config.MaxCombatDefeatHealthLoss PV</li>
                            </ul>
                        </div>

                        <div class="card p-4 transform hover:scale-[1.01] transition-transform duration-300">
                            <h4 class="text-lg mb-2 font-medieval text-medieval-accent">Fouiller</h4>
                            <ul class="text-medieval-light space-y-1 list-disc list-inside text-sm">
                                <li>Trésor : +@_config.MinTreasurePoints à +@_config.MaxTreasurePoints points</li>
                                <li>Potion : +@_config.MinPotionHealthGain à +@_config.MaxPotionHealthGain points de vie</li>
                                <li>Piège : @_config.MinTrapPoints à @_config.MaxTrapPoints points et @_config.MinTrapHealthLoss à @_config.MaxTrapHealthLoss PV</li>
                            </ul>
                        </div>

                        <div class="card p-4 transform hover:scale-[1.01] transition-transform duration-300">
                            <h4 class="text-lg mb-2 font-medieval text-medieval-accent">Fuir</h4>
                            <ul class="text-medieval-light space-y-1 list-disc list-inside text-sm">
                                <li>@_config.MinFleePoints à @_config.MaxFleePoints points</li>
                                <li>Aucun dégât</li>
                                <li>Passage sûr à la salle suivante</li>
                            </ul>
                        </div>
                    </div>
                }
                else
                {
                    <p class="text-medieval-light text-center text-sm">Chargement des règles du jeu...</p>
                }
            </div>
        </div>
    </div>

    <div class="text-center mt-4">
        <MedievalButton 
            Text="Commencer l'aventure"
            OnClick="OnStartGame"
            Disabled="@IsLoading"
            IsLarge="false"
        />
    </div>
</div>

@code {
    [Parameter]
    public bool IsLoading { get; set; }

    [Parameter]
    public EventCallback OnStartGameClick { get; set; }

    private GameRewards? _config;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            _config = await GameService.GetGameRewardsAsync();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Erreur lors du chargement de la configuration: {ex.Message}");
        }
    }

    private async Task OnStartGame()
    {
        await OnStartGameClick.InvokeAsync();
    }
}